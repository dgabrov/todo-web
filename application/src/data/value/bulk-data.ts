export default interface BulkData {
    selectedOwner: boolean;
    ownerId: string;

    selectedContext: boolean;
    context: string;

    selectedProject: boolean;
    project: string;

    selectedDue: boolean;
    due: string;

    selectedPriority: boolean;
    priority: string;

}

