@application/src/controls/Pwd.tsx
- password fields should be invisible, instead you show ***
- there is a link to the right side that says "show" and that will show the password value instead of the *** and will toggle
- edit shall show password field for the password instead of text field, however toggleable as well with a button on the right
